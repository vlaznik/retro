<%= form_for(retro_game) do |f| %>
  <% if retro_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retro_game.errors.count, "error") %> prohibited this retro_game from being saved:</h2>

      <ul>
      <% retro_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
